# AI/ML Price Auto Cluster — คำอธิบายสำหรับนำเสนออาจารย์

ฟีเจอร์นี้ใช้ **K-Means Clustering จริง** (ไม่ใช่ threshold ราคาที่ hardcode) เพื่อแบ่งสินค้าเครื่องดื่มในร้านออกเป็น 3 กลุ่มตามราคา: **Budget / Standard / Premium** โดยคำนวณที่ backend ทุกครั้งที่มีการเรียก `GET /api/products` แบบ real-time ไม่มีการเก็บผลลัพธ์ไว้ในฐานข้อมูล

โค้ดทั้งหมดอยู่ที่ `backend/services/priceClustering.js` เขียนด้วย Pure JavaScript ไม่ได้เพิ่ม library ML ใดๆ เข้าไปในโปรเจกต์

---

## 1. Input คืออะไร

Input คือ **array ของตัวเลขราคา (price)** ของสินค้าที่ยัง active อยู่เท่านั้น (`Inventory.is_active = 1`) ดึงมาจากฐานข้อมูลจริงด้วยคำสั่ง:

```sql
SELECT id, price FROM Inventory WHERE is_active = 1
```

ใช้ **ราคาเป็น feature เดียว** ตามที่โจทย์กำหนด (1-dimensional clustering) ไม่ได้ใช้ยอดขาย จำนวน stock หรือข้อมูลอื่นที่ไม่มีในฐานข้อมูลจริงมาปนเลย

## 2. Initial Centroids ตั้งค่าอย่างไร

แทนที่จะสุ่มจุดเริ่มต้นแบบ random (ซึ่งจะทำให้ผลลัพธ์เปลี่ยนไปทุกครั้งที่รีเฟรช ไม่เหมาะกับการ demo และไม่ consistent) โค้ดนี้ใช้วิธี **กระจาย centroid เริ่มต้นแบบเท่าๆ กัน (evenly spaced)** ครอบคลุมช่วงราคาต่ำสุด–สูงสุดที่มีอยู่จริง

สำหรับ K = 3: centroid เริ่มต้นจะเป็น **[ราคาต่ำสุด, ราคากึ่งกลาง, ราคาสูงสุด]** พอดี

จุดสำคัญ: นี่เป็นแค่ "จุดเริ่มต้นในการเดา" ไม่ใช่กฎทางธุรกิจที่ตายตัว — อัลกอริทึมจะปรับ centroid เหล่านี้ด้วยข้อมูลจริงในขั้นตอนถัดไป

## 3. Assignment Step ทำงานอย่างไร

ราคาทุกตัวจะถูกจับคู่กับ centroid ที่ **ใกล้ที่สุด** โดยวัดจากระยะห่างสัมบูรณ์ (`|price - centroid|`) — สินค้าไหนราคาใกล้ centroid ไหนที่สุด ก็จะถูกจัดเข้ากลุ่มนั้น

## 4. Update Step ทำงานอย่างไร

หลังจัดกลุ่มเสร็จในแต่ละรอบ centroid ของแต่ละกลุ่มจะถูกคำนวณใหม่เป็น **ค่าเฉลี่ย (mean) ของราคาทุกตัวที่อยู่ในกลุ่มนั้น** ทำให้ centroid ขยับเข้าหาจุดกึ่งกลางที่แท้จริงของกลุ่มตัวเองมากขึ้นเรื่อยๆ

## 5. หยุดคำนวณเมื่อไหร่ (Stopping Condition)

ขั้นตอนที่ 3–4 จะวนซ้ำจนกว่า **ทั้งสองเงื่อนไขนี้เป็นจริงพร้อมกัน**:

- ไม่มีสินค้าตัวไหนย้ายกลุ่มอีกแล้ว (assignment คงที่)
- centroid ทุกตัวขยับน้อยกว่าค่า tolerance ที่กำหนด (1e-6) — แทบไม่ขยับแล้ว

หรือจนกว่าจะครบ **100 รอบ** (safety cap กันไม่ให้ loop ค้างตลอดกาล) — ในทางปฏิบัติข้อมูลราคา 1 มิติแบบนี้จะลู่เข้า (converge) ภายในไม่กี่รอบเท่านั้น ตัว cap 100 แทบไม่เคยเป็นสาเหตุที่ทำให้หยุดจริง

## 6–8. ทำไม centroid ต่ำสุด/กลาง/สูงสุด ถึงถูกตั้งชื่อ Budget/Standard/Premium

ตัว K-Means เองไม่รู้จักคำว่า "Budget" หรือ "Premium" — มันคืนแค่ตัวเลข centroid 3 ค่า กับ assignment ของแต่ละสินค้า ขั้นตอนสุดท้ายของโค้ดคือ **นำ centroid ทั้ง 3 มาเรียงจากน้อยไปมาก (sort ascending) แล้ว map ตามลำดับ**:

- **Centroid ที่มีค่าต่ำสุด → Budget** เพราะกลุ่มนี้คือกลุ่มสินค้าที่ราคาเฉลี่ยถูกที่สุดในร้าน ณ ขณะนั้น
- **Centroid ตรงกลาง → Standard** เพราะเป็นกลุ่มราคาปานกลาง อยู่ระหว่างสองกลุ่มที่เหลือ
- **Centroid ที่มีค่าสูงสุด → Premium** เพราะเป็นกลุ่มสินค้าที่ราคาเฉลี่ยแพงที่สุดในร้าน ณ ขณะนั้น

จุดสำคัญคือชื่อกลุ่มไม่ได้ผูกกับ index หรือลำดับที่ centroid เริ่มต้น แต่ผูกกับ **อันดับค่าราคาจริงหลัง converge แล้ว** ดังนั้นไม่ว่าจะมีสินค้าใหม่เพิ่มเข้ามาหรือราคาสินค้าเปลี่ยนไป ระบบจะคำนวณกลุ่มใหม่และตั้งชื่อใหม่ให้ถูกต้องเสมอแบบอัตโนมัติ — นี่คือหัวใจของการเป็น "Auto Cluster" ตามชื่อฟีเจอร์

---

## ตัวอย่าง JSON Response ก่อน/หลัง เพิ่ม priceTier

### ก่อนแก้ (เดิม) — `GET /api/products`

```json
[
  {
    "id": 4,
    "name": "ชาไข่มุกพรีเมียม",
    "price": 65,
    "stock": 8,
    "category": "ชานม",
    "image_url": null,
    "description": null
  },
  {
    "id": 1,
    "name": "ชาไทย",
    "price": 25,
    "stock": 10,
    "category": "ชาไทย",
    "image_url": null,
    "description": null
  }
]
```

### หลังแก้ (ใหม่) — `GET /api/products` (มีฟิลด์ `priceTier` เพิ่มเข้ามาเท่านั้น)

```json
[
  {
    "id": 4,
    "name": "ชาไข่มุกพรีเมียม",
    "price": 65,
    "stock": 8,
    "category": "ชานม",
    "image_url": null,
    "description": null,
    "priceTier": "Premium"
  },
  {
    "id": 1,
    "name": "ชาไทย",
    "price": 25,
    "stock": 10,
    "category": "ชาไทย",
    "image_url": null,
    "description": null,
    "priceTier": "Budget"
  }
]
```

ผลลัพธ์นี้มาจากการรัน mock test จริงกับสินค้า 4 ตัว (ราคา 25 / 30 / 45 / 65 บาท) ซึ่งจัดกลุ่มได้เป็น 25→Budget, 30→Budget, 45→Standard, 65→Premium ตรงตามสัดส่วนราคาจริง ไม่ใช่ threshold ที่ตั้งตายตัวไว้ล่วงหน้า

Field อื่นๆ ทั้งหมด (`id`, `name`, `price`, `stock`, `category`, `image_url`, `description`) **เหมือนเดิมทุกประการ** — ไม่มีการลบ ไม่มีการเปลี่ยนชื่อ ไม่มีการเปลี่ยน type ใดๆ Frontend ที่ใช้ field เดิมจะไม่พังแม้แต่นิดเดียว
